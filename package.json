{
  "name": "kamigotchi",
  "version": "1.0.0",
  "private": true,
  "license": "MIT",
  "workspaces": [
    "packages/contracts",
    "packages/client"
  ],
  "scripts": {
    "dev": "run-pty % yarn dev:node % yarn dev:client % yarn dev:contracts",
    "client:build": "yarn workspace client run build",
    "client:dev": "yarn workspace client run dev",
    "client:start": "yarn workspace client run start",
    "contracts:node": "yarn workspace contracts run devnode",
    "contracts:dev": "yarn workspace contracts run dev"
  },
  "devDependencies": {
    "buffer": "^5.5.0",
    "events": "^3.1.0",
    "npm-run-all": "^4.1.5",
    "path-browserify": "^1.0.0",
    "punycode": "^1.4.1",
    "querystring-es3": "^0.2.1",
    "run-pty": "^4.0.3",
    "url": "^0.11.0"
  },
  "dependencies": {}
}