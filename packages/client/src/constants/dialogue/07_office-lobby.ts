import { DialogueNode } from '.';

const chair: DialogueNode = {
  index: 71,
  text: ['While obviously aged, the couch is still in respectable condition.'],
};

const cabinet: DialogueNode = {
  index: 72,
  text: ['A cabinet. Inside...'],
};

export default [chair, cabinet];
